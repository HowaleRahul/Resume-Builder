{
  "name": "careerflow-ai",
  "version": "1.0.0",
  "description": "Full-stack resume builder with AI",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build && npm --prefix backend run build || true",
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "install": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}

